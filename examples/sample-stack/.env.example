# Copy to .env and fill in real values before running the sample stack.
ANTHROPIC_API_KEY=sk-ant-...
GITHUB_TOKEN=ghp_...

# Postgres password (matches passwordEnv in loop-e2e.config.yaml)
DB_PASS=testpass

# App credentials (match usernameEnv/passwordEnv in loop-e2e.config.yaml)
APP_USER=user@example.com
APP_PASS=changeme
